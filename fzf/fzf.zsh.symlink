# Setup fzf
# ---------
if [[ ! "$PATH" == */home/toni/.fzf/bin* ]]; then
  export PATH="${PATH:+${PATH}:}/home/toni/.fzf/bin"
fi

# Auto-completion
# ---------------
[[ $- == *i* ]] && source "/home/toni/.fzf/shell/completion.zsh" 2> /dev/null

# Key bindings
# ------------
source "/home/toni/.fzf/shell/key-bindings.zsh"
