# this only installs the custom remote for ur, not ur itself
# restart server (on the dashboard if needed) in order to activate this remote
sudo ln -s $CONFIG/unifiedremote/custom/ /opt/urserver/remotes/custom 
